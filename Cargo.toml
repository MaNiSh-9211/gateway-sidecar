[package]
name = "config-sidecar"
version = "0.1.0"
edition = "2021"

[dependencies]
ureq = { version = "2.12.1", features = ["json"] }
serde_json = "1.0"

[profile.release]
opt-level     = 3
codegen-units = 1
strip         = true
